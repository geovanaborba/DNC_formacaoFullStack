var slide = {
    origin: 'left',
    distance: '100px',
    scale: 0.80,
    duration: 2000,
    reset: true,
    delay: 300,
};

ScrollReveal().reveal('header h1', slide);
ScrollReveal().reveal('header h5', slide);

